version: "3"
services:
  web:
    image: nginx:1.22
    volumes:
    - ./frontend/nginx.conf:/etc/nginx/nginx.conf:ro
    - ./frontend/static:/var/www/static
    ports:
    - 80:80
  todo-api-service:
    build: ./backend
